name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Publish win-x86 standalone (default — Plustek PS4080U TWAIN)
        run: >
          dotnet publish src/Sohoa.ScanAgent/Sohoa.ScanAgent.csproj
          -c Release
          -r win-x86
          --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -p:EnableCompressionInSingleFile=true
          -o publish-x86

      - name: Publish win-x86 slim (optional — needs .NET 8 Desktop Runtime x86)
        run: >
          dotnet publish src/Sohoa.ScanAgent/Sohoa.ScanAgent.csproj
          -c Release
          -r win-x86
          --self-contained false
          -p:PublishSingleFile=true
          -o publish-x86-slim

      - name: Publish win-x64 (optional, for 64-bit TWAIN drivers)
        run: >
          dotnet publish src/Sohoa.ScanAgent/Sohoa.ScanAgent.csproj
          -c Release
          -r win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -p:EnableCompressionInSingleFile=true
          -o publish-x64

      - name: Package zips
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          $x86 = "SohoaScanAgent-$version-win-x86.zip"
          $x86Slim = "SohoaScanAgent-$version-win-x86-slim.zip"
          $x64 = "SohoaScanAgent-$version-win-x64.zip"
          Compress-Archive -Path publish-x86/SohoaScanAgent.exe -DestinationPath $x86 -Force
          Compress-Archive -Path publish-x86-slim/SohoaScanAgent.exe -DestinationPath $x86Slim -Force
          Compress-Archive -Path publish-x64/SohoaScanAgent.exe -DestinationPath $x64 -Force
          "ZIP_X86=$x86" >> $env:GITHUB_ENV
          "ZIP_X86_SLIM=$x86Slim" >> $env:GITHUB_ENV
          "ZIP_X64=$x64" >> $env:GITHUB_ENV

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Sohoa Scan Agent ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            ${{ env.ZIP_X86 }}
            ${{ env.ZIP_X86_SLIM }}
            ${{ env.ZIP_X64 }}
          body: |
            ## Cài đặt trên máy quét (Windows)

            ### Plustek PS4080U → dùng **win-x86**

            Driver TWAIN Plustek chỉ có bản **32-bit**.

            1. Tải **`${{ env.ZIP_X86 }}`** (standalone, không cần cài .NET)
            2. Giải nén → chạy `SohoaScanAgent.exe`
            3. Kiểm tra `http://127.0.0.1:18612/health` → `processBitness: "x86"` và `twainSources` không rỗng

            **Tùy chọn nhẹ RAM hơn:** **`${{ env.ZIP_X86_SLIM }}`** — cần [.NET 8 Desktop Runtime x86](https://dotnet.microsoft.com/download/dotnet/8.0).

            Agent: `http://127.0.0.1:18612` (localhost only).
